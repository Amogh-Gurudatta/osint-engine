---
trigger: always_on
---

All code must be written in Python 3.12+.

Strictly use asyncio and aiohttp for any web requests.

Use NetworkX for all graph data structures.

Do not use any LLM APIs or wrappers. The logic must be purely programmatic.

Keep the code highly modular: separate data ingestion, graph logic, and UI into different files.